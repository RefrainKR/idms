# 스탭업 주요 구간별 효과

- 상위 색인: [NEXTROAD.md](NEXTROAD.md)
- 항목 번호: 2
- 상태: 선행 UI 개선 검토 후 진행
- 대상 버전 계열: `v.1.12.X`
- 구현 승인: 아직 없음
- 마지막 정리: 2026-08-29

## 목적

3성 `일반 vs 스탭업` 그래프에 이미 계산되는 횟수별 확률을 이용해, 각 주회의 기본 뽑기·Step4·주회 보상이 성공 또는 폭사 확률에 얼마나 기여하는지 주요 구간별로 보여줍니다.

단순히 이전 주회보다 몇 %p 변했는지만 표시하지 않고 다음 요소를 구분하는 것이 핵심입니다.

- 해당 구간의 기본 뽑기
- Step4의 마지막 확률 증가 대상 뽑기
- 랜덤 티켓
- 셀렉 티켓
- 일반+스탭업 공유 200스택 천장

이 항목은 그래프를 보면서 입력을 반복 조정하기 어려운 기본 UI 문제를 먼저 해결한 뒤 진행합니다. 선행 항목은 [NEXTROAD_01_GACHA_QUICK_CONTROLS.md](NEXTROAD_01_GACHA_QUICK_CONTROLS.md)입니다.

## 필요한 이유

현재 효율 그래프는 40회 단위 지점을 강조하고 툴팁으로 확률을 확인할 수 있지만, 사용자가 각 지점을 직접 눌러 비교해야 합니다. 특히 모바일에서는 주회별 변화와 그 원인을 한눈에 파악하기 어렵습니다.

이 표는 다음 질문에 답하는 보조 정보입니다.

> 스탭업의 확률상 이점 가운데 기본 뽑기, Step4, 랜덤 티켓과 셀렉 티켓이 각각 얼마나 기여하는가?

## 확정한 용어와 토글 의미

- `랜덤 티켓`: 게임의 랜덤 픽업 획득 보상이며 중복 가능성이 있습니다.
- `셀렉 티켓`: PJ 3주차 등에서 주어지는 선택 획득 보상이며 천장 개념에 포함합니다.
- `200스택 천장`: 일반+스탭업 공유 200스택으로 받는 선택 획득 보상입니다.
- `천장` 토글: 셀렉 티켓과 200스택 천장을 함께 포함하거나 제외합니다.
- `랜덤 티켓` 토글: 랜덤 티켓만 별도로 포함하거나 제외합니다.
- `Step4 확률` 토글: Step4 확률 증가만 별도로 포함하거나 제외합니다.

셀렉 티켓과 200스택 천장은 획득 경로는 다르지만 모두 원하는 대상을 선택 획득하는 동일한 천장 계열입니다. 표에서는 출처를 구분하되 기존 `천장` 토글 구조는 유지합니다.

## 표시 위치와 화면 순서

첫 적용 위치는 3성 가챠의 `일반 vs 스탭업` 서브탭입니다.

```text
[픽업 획득] [총 획득] [일반 vs 스탭업] [무돌]

[천장] [랜덤 티켓] [Step4 확률]    [목표 확률 90 %] [Best (성공)]

[일반 vs 스탭업 차트]

[주요 구간별 효과 표]

[공통 결과 요약]
```

`sub-tab-nav → option-button-container → 차트`의 위치가 서브탭을 전환할 때 바뀌지 않는 현재 감각을 유지합니다.

목표 확률 입력을 차트 위의 별도 행으로 만들지 않습니다. 기존 `option-button-container`의 오른쪽 그룹에 작은 숫자 입력으로 배치해 차트 시작 높이를 유지합니다.

다른 서브탭과 효율 탭의 옵션 행은 다음처럼 같은 높이를 유지하는 방향입니다.

```text
픽업 획득:
[천장] [랜덤 티켓] [Step4 확률]                      [개별]

일반 vs 스탭업:
[천장] [랜덤 티켓] [Step4 확률]    [목표 확률 90 %] [Best (성공)]
```

모바일에서는 기존 반응형 구조에 맞춰 두 줄로 표시합니다.

```text
[천장] [랜덤 티켓] [Step4 확률]
[목표 확률 90 %] [Best (성공)]
```

목표 확률 입력에는 3성 전용 CSS 대신 생일·콜라보에서도 재사용할 수 있는 공통 숫자 제어 클래스를 사용합니다.

## 구간 표기

주회 구간은 횟수보다 주회를 먼저 표시합니다.

- `1주(40회)`
- `2주(80회) · 랜덤 티켓`
- `3주(120회) · 셀렉 티켓`
- `천장(200회)`

정규 배너는 2주까지만 있으므로 3주 행을 표시하지 않습니다. PJ는 3주 행을 표시합니다. 사용자 설정에서는 실제 `maxLoops`와 `loopRewards`를 기준으로 행을 동적으로 생성합니다.

200회는 정규/PJ에서 스탭업 주회가 끝난 뒤 일반으로 전환된 상태이므로 무조건 `5주(200회)`로 부르지 않습니다.

- 정규: `천장(200회)` — 스탭업 80회 + 일반 120회
- PJ: `천장(200회)` — 스탭업 120회 + 일반 80회

사용자 설정에서 200회가 실제 주회 종료 지점과 겹친다면 같은 횟수의 행을 중복 생성하지 않고 주회 보상과 200스택 천장 효과를 한 행에서 구분합니다.

## 권장 표 구성

모바일 폭을 고려해 효과마다 별도 열을 만들지 않고 `효과 구성` 한 열에 모읍니다.

| 구간 | 최종 확률 | 이전 대비 | 효과 구성 |
|---|---:|---:|---|
| 1주(40회) | 예시값 | 예시값 | 기본 +… · Step4 +… |
| 2주(80회) | 예시값 | 예시값 | 기본 +… · Step4 +… · 랜덤 티켓 +… |
| 3주(120회) | 예시값 | 예시값 | 기본 +… · Step4 +… · 셀렉 티켓 +… |
| 천장(200회) | 예시값 | 예시값 | 기본 +… · 200스택 천장 +… |

문서의 값은 형식 예시일 뿐이며 실제 숫자는 현재 입력과 토글 상태에서 계산합니다.

토글로 제외된 요소는 `+0.000%p` 대신 다음처럼 표시하는 방안을 우선 검토합니다.

- `Step4 제외`
- `랜덤 티켓 제외`
- `셀렉 티켓 제외`
- `200스택 천장 제외`

## 효과 분해 계산

각 주회 행은 이전 주요 구간의 실제 상태에서 시작하고 게임의 적용 순서에 맞춰 단계별 상태를 만듭니다.

1. 이전 주요 구간의 모든 실제 효과가 반영된 DP에서 시작합니다.
2. 다음 구간까지 뽑기를 진행하되 마지막 Step4 대상 뽑기를 일반 픽업 확률로 처리한 기준 상태를 만듭니다.
3. 마지막 대상 뽑기를 Step4 확률로 처리한 상태와 비교해 Step4 추가 효과를 구합니다.
4. 해당 주회의 랜덤 티켓 또는 셀렉 티켓을 적용해 보상 효과를 구합니다.
5. 공유 200스택에 도달했다면 200스택 천장을 적용해 천장 효과를 구합니다.

Best 모드의 성공 확률을 `P`라고 할 때 다음처럼 계산합니다.

```text
기본 뽑기 효과 = P(기본 구간 종료) - P(이전 주요 구간)
Step4 효과 = P(Step4 적용) - P(기본 구간 종료)
주회 보상 효과 = P(주회 보상 적용) - P(Step4 적용)
200스택 천장 효과 = P(최종 상태) - P(주회 보상 적용)
```

따라서 다음 합이 정확히 성립해야 합니다.

```text
이전 주요 구간 대비 전체 변화
= 기본 뽑기 효과
+ Step4 효과
+ 주회 보상 효과
+ 200스택 천장 효과
```

이 기여도는 현재 입력 상태와 실제 적용 순서를 기준으로 한 조건부 값입니다. 랜덤 티켓이나 Step4의 모든 배너에 통용되는 고정 가치로 표현하지 않습니다.

## 구현 책임 경계

- 주요 구간과 보상 종류를 결정하는 게임 규칙은 View에서 추론하지 않습니다.
- `EfficiencyCalculator`와 기존 `ProbabilityEngine` 전이를 재사용해 단계별 상태를 계산합니다.
- ViewModel은 계산 결과와 표시용 구간 메타데이터를 View에 전달합니다.
- `GachaResultView`는 전달받은 결과를 렌더링하며 확률 규칙을 다시 구현하지 않습니다.
- 기존 일반·스탭업 그래프, CDF, 목표 확률 계산 결과는 변경하지 않습니다.

현재의 전체 Step4/랜덤/천장 토글 결과를 단순히 서로 빼는 방식은 사용하지 않습니다. 전체 토글은 모든 주회에 영향을 주므로 특정 주회의 기여도를 분리할 수 없기 때문입니다.

## 첫 구현 범위 후보

- 3성 정규/PJ 및 사용자 주회 설정
- `일반 vs 스탭업` 탭의 주요 구간별 효과 표
- 옵션 행 안의 작은 목표 확률 입력
- Best/Worst 현재 모드에 맞는 확률 및 증감 표시
- 데스크톱과 모바일에서 차트 시작 위치 유지

이번 항목에서 바로 일반화하지 않을 대상:

- 생일 주요 구간 표
- 콜라보 주요 구간 표
- 2성 주요 구간 표
- 랜덤 티켓이나 셀렉 티켓의 보편적 재화 가치
- 새로운 확률 엔진 또는 게임 규칙 변경

## 추가로 결정할 사항

1. 표를 Best/Worst 토글에 맞춰 함께 전환할지, 성공 확률만 고정 표시할지
2. 목표 확률 입력의 옵션 행 이동을 생일·콜라보에도 같은 작업에서 적용할지
3. 표를 항상 펼쳐둘지, 작은 화면에서 접기 기능을 제공할지
4. 최종 확률과 각 효과의 소수점 표시 자릿수
5. 사용자 설정에서 200회에 Step4·주회 보상·200스택 천장이 동시에 발생할 때의 최종 라벨 형식

## 현재 결론

기능의 필요성과 기본 배치는 타당하다고 판단합니다. 다만 위의 추가 결정 사항을 개발자와 검토한 뒤에만 구현 범위로 확정합니다. 이 문서는 아직 구현 승인 문서가 아닙니다.
