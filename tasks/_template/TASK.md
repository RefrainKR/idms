---
status: planning
planning_role: Sol
implementation_role: undecided
review_role: Sol
branch: null
version: null
next_role: Sol
---

# 작업명

## 목적

## 구현 담당 판단

- 담당 역할: Sol 직접 수행 / Terra 인계
- 복잡성·불확실성:
- 인계 비용 대비 이점:
- 판단 근거:

## 버전 및 브랜치

- 현재 배포 버전:
- 현재 배포 브랜치: `v.1.<line>.<patch>`
- 계열 통합 브랜치: `v.1.<line>.X`
- 작업 버전: `1.<line>.<patch>`
- 작업/릴리스 후보 브랜치: `v.1.<line>.<patch>`
- 배포 후 복귀 브랜치: `v.1.<line>.<patch>`
- 변경 분류: 새 기능·기능 교체 / 버그·소규모 변경
- `main` 아님을 확인:

### 예정 브랜치 흐름

- 패치: 현재 정확 버전 → 현재 계열 `X` → 다음 정확 버전 → 현재 계열 `X` → `main` → 다음 정확 버전으로 복귀
- 새 계열: 현재 정확 버전 → 현재 계열 `X` → `main` → 다음 계열 `X`; 첫 배포 후 `main` → 새 정확 버전으로 복귀

## 허용 범위

## 금지 범위

## 완료 조건
